#include <stdio.h>
#define MAX 1000

int main(){

    char c;
    char current[MAX];
    int i;
    
    // Removes blanks, tabs, new lines.
    while((c = getchar()) != EOF){
        if(c != ' ' && c != '\t' && c != '\n'){
            current[i++] = c;
            }
    }

    printf("%s", current);

    return 0;
}
